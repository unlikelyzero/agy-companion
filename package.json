{
  "name": "agy-companion",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Use Google's Antigravity CLI (agy) from Claude Code to review code or delegate tasks.",
  "author": "John Hill (https://github.com/unlikelyzero)",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "version:check": "node scripts/version.mjs check",
    "version:current": "node scripts/version.mjs current",
    "version:notes": "node scripts/version.mjs notes",
    "version:set": "node scripts/version.mjs set"
  }
}
